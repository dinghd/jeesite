<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.sms.dao.StudentDao">

  <sql id="Base_Column_List" >
    id, name, no, age, grade, stu_class, reserved1, reserved2, create_by, create_date, update_by, update_date, remarks, del_flag
  </sql>

  <select id="get" resultType="com.thinkgem.jeesite.modules.sms.entity.Student">
    select
    <include refid="Base_Column_List" />
    from sms_student
    where id = #{id}
  </select>

  <select id="getByName" resultType="com.thinkgem.jeesite.modules.sms.entity.Student">
    select
    <include refid="Base_Column_List" />
    from sms_student
    where name = #{name}
  </select>

  <select id="findList" resultType="com.thinkgem.jeesite.modules.sms.entity.Student">
    SELECT
    <include refid="Base_Column_List" />
    FROM sms_student
    <where>
      del_flag = #{delFlag}
      <if test="name != null and name != ''">
        AND name LIKE
        <if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
        <if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
        <if test="dbName == 'mysql'">CONCAT('%', #{name}, '%')</if>
      </if>
    </where>
    <choose>
      <when test="page !=null and page.orderBy != null and page.orderBy != ''">
        ORDER BY ${page.orderBy}
      </when>
      <otherwise>
        ORDER BY update_date DESC
      </otherwise>
    </choose>
  </select>

  <select id="findAllList" resultType="com.thinkgem.jeesite.modules.sms.entity.Student">
    SELECT
      <include refid="Base_Column_List" />
    FROM sms_student
    WHERE del_flag = #{DEL_FLAG_NORMAL}
    ORDER BY update_date DESC
  </select>

  <insert id="insert">
    insert into sms_student (
    id,
    name,
    no,
    age,
    grade,
    stu_class,
    reserved1,
    reserved2,
    create_by,
    create_date,
    update_by,
    update_date,
    remarks,
    del_flag
    )
    values (
    #{id},
    #{name},
    #{no},
    #{age},
    #{grade},
    #{stuClass},
    #{reserved1},
    #{reserved2},
    #{createBy.id},
    #{createDate},
    #{updateBy.id},
    #{updateDate},
    #{remarks},
    #{delFlag}
    )
  </insert>

  <update id="update">
    update sms_student
    set name = #{name,jdbcType=VARCHAR},
      no = #{no,jdbcType=VARCHAR},
      age = #{age,jdbcType=INTEGER},
      grade = #{grade,jdbcType=VARCHAR},
      stu_class = #{stuClass,jdbcType=VARCHAR},
      reserved1 = #{reserved1,jdbcType=VARCHAR},
      reserved2 = #{reserved2,jdbcType=INTEGER},
	  update_by = #{updateBy.id},
	  update_date = #{updateDate},
	  remarks = #{remarks},
	  del_flag = #{delFlag}
    where id = #{id,jdbcType=INTEGER}
  </update>

  <update id="delete">
    UPDATE sms_student SET
    del_flag = #{delFlag}
    WHERE id = #{id}
  </update>

</mapper>